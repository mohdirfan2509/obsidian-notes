# Note Conversion Spec (MUST FOLLOW)

Workspace: /home/irfan/Ubuntu-workspace/JAVA-MICROSERVICES-SEM-4
- Raw: raw-files/
- Output MD: NOTES/
- Images already in: IMAGES/

## For each note entry in your batch JSON:
1. If `src` is not null: READ the entire raw-files/{src} content. Preserve ALL meaningful content.
2. If `src` is null (image-only): do NOT invent mentor lecture content. Brief topic identification only.
3. Write NOTES/{dest} exactly as specified.

## Required structure for TXT-based notes:

```
# {day} — {date} — {title}

---

## 🧩 ... sections with emoji headings covering ALL original content ...

(format: short paragraphs, bullets, numbered lists, tables, fenced code, text flows)

---

## 🤖 AI Points

1. **Production consideration:** ...
2. **Industry practice:** ...
3. **Common mistake:** ...
4. **Interview insight:** ...
5. **Real-world connection:** ...  (or Modern Spring Boot connection if relevant)

---

## 💻 My Codes


  
## 🖼️ Image

![alt](../IMAGES/filename.png)
```

Between `## 💻 My Codes` and `## 🖼️ Image` leave approximately two blank lines.

If images list is empty, still include `## 🖼️ Image` with no image embeds (or a short note that no matching lecture image is available — prefer empty section with no fake images).

## Image-only notes structure:

```
# {day} — {date} — {title}

---

## 🧩 {Topic}

This lecture date has image material only (no textual mentor `.txt` note).
Topic identified from filename/image: ...
Do not invent mentor lecture prose.

---

## 🤖 AI Points
(exactly 5 topic-specific points)

---

## 💻 My Codes


  
## 🖼️ Image

![...](../IMAGES/...)
```

## Formatting rules:
- Correct spelling/grammar; keep all technical info
- Use ```java ```xml ```sql ```bash ```properties ```json ```html ```text as appropriate
- Inline code for class/method/annotation/config names
- Convert tables to Markdown tables
- Preserve teaching flows as ```text blocks with arrows
- Emoji ONLY on headings/subheadings
- Relative image paths only: ../IMAGES/...
- Exactly 5 AI Points — unique to THIS lecture, not generic filler
- Empty My Codes (no placeholders)
- Do NOT summarize/shorten mentor content
- Technical corrections: keep original idea + clarify with blockquote **Correction:** if needed
- Filename dest is authoritative; H1 should be: `# Day-XX — DD-MM-YYYY — Title`

## After writing all notes in your batch:
Print a checklist of files written and any issues.
